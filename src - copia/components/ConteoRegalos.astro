<!-- ═══════════════════════════════════════════════════════════ --><!-- 🎁 SECCIÓN: CONTEO DE REGALOS                              --><!-- ═══════════════════════════════════════════════════════════ -->
<section class="relative w-full pt-20 pb-12 sm:py-24 md:py-28 lg:py-36">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <!-- 📇 CARD DE CONTEO DE REGALOS - HORIZONTAL -->
        <div class="flex justify-center">
            <div
                class="relative w-full max-w-md sm:max-w-lg md:max-w-xl"
                id="card-conteo"
            >
                <!-- BOTÓN DECORATIVO SUPERIOR -->
                <div
                    class="absolute z-20"
                    style="top: -30px; left: 50%; transform: translateX(-50%);"
                >
                    <div class="w-16 h-16" id="boton-decorativo-conteo">
                        <img
                            src="/imagenes/boton.png"
                            alt="Decoración"
                            class="w-full h-full object-contain"
                        />
                    </div>
                </div>

                <div
                    class="relative rounded-2xl shadow-xl overflow-hidden"
                    style="background-color: #e8dcc8;"
                >
                    <!-- IMAGEN DE FONDO -->
                    <img
                        src="/imagenes/fondorojo.png"
                        alt="Fondo conteo de regalos"
                        class="absolute inset-0 w-full h-full object-cover opacity-90"
                    />

                    <!-- Contenido de la card -->
                    <div
                        class="relative px-4 pt-14 pb-8 sm:px-8 sm:pt-16 sm:pb-10 md:px-10 md:pt-16 md:pb-12 z-10"
                    >
                        <!-- TÍTULO: CONTEO DE REGALOS -->
                        <div class="mb-2">
                            <h2
                                class="font-great-vibes text-4xl sm:text-4xl md:text-5xl texto-conteo text-center"
                                id="titulo-conteo"
                            >
                                Conteo de Regalos
                            </h2>
                        </div>

                        <!-- CONTENIDO INFERIOR CON ICONO Y TEXTOS -->
                        <div class="flex items-center justify-between gap-4">
                            <!-- LADO IZQUIERDO: ICONO DE REGALO -->
                            <div class="flex-shrink-0 ml-4 sm:ml-6 md:ml-8">
                                <div
                                    class="w-24 h-24 sm:w-20 sm:h-20 md:w-24 md:h-24 icono-conteo-color"
                                >
                                    <img
                                        src="/imagenes/rega.png"
                                        alt="Regalo"
                                        class="w-full h-full object-contain"
                                        id="icono-regalo"
                                    />
                                </div>
                            </div>

                            <!-- LADO DERECHO: TEXTOS Y BOTÓN -->
                            <div class="flex-1 text-center">
                                <!-- FECHA -->
                                <div class="mb-2">
                                    <p
                                        class="font-crimson text-xl sm:text-lg md:text-xl texto-conteo font-semibold"
                                        id="fecha-conteo"
                                    >
                                        Domingo 13 de septiembre
                                    </p>
                                </div>

                                <!-- LUGAR -->
                                <div class="mb-2 leading-tight">
                                    <p
                                        class="font-crimson text-xl sm:text-sm md:text-base texto-conteo"
                                        id="lugar-conteo"
                                    >
                                        Salón de Eventos "La Corona"<br />
                                        Calle Otero de la Vega<br />
                                        y Héroes del Acre
                                    </p>
                                </div>

                                <!-- BOTÓN VER UBICACIÓN -->
                                <div class="mt-4">
                                    <a
                                        href="https://maps.app.goo.gl/7HoijkwD7oAwkQn48"
                                        target="_blank"
                                        rel="noopener noreferrer"
                                        class="inline-block px-4 py-2 sm:px-6 sm:py-2.5 border-2 rounded-full font-crimson text-base sm:text-sm uppercase tracking-wider transition-all duration-300 btn-ubicacion-conteo font-semibold"
                                        id="btn-ubicacion-conteo"
                                    >
                                        VER UBICACIÓN
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- ═══════════════════════════════════════════════════════════ -->
<!-- 🎬 ANIMACIONES CON GSAP                                     -->
<!-- ═══════════════════════════════════════════════════════════ -->
<script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";

    gsap.registerPlugin(ScrollTrigger);

    if (typeof window !== "undefined") {
        document.addEventListener("DOMContentLoaded", () => {
            // 🎯 BOTÓN DECORATIVO
            gsap.fromTo(
                "#boton-decorativo-conteo",
                { opacity: 0, scale: 0, rotation: -180 },
                {
                    opacity: 1,
                    scale: 1,
                    rotation: 0,
                    duration: 1,
                    ease: "back.out(1.7)",
                    scrollTrigger: {
                        trigger: "#card-conteo",
                        start: "top 80%",
                        toggleActions: "play none none reset",
                    },
                },
            );

            // 🎯 ANIMACIÓN ELEGANTE DE ENTRADA
            gsap.fromTo(
                "#card-conteo",
                {
                    opacity: 0,
                    scale: 0.85,
                    y: 60,
                    rotationY: -15,
                },
                {
                    opacity: 1,
                    scale: 1,
                    y: 0,
                    rotationY: 0,
                    duration: 1.2,
                    ease: "power3.out",
                    scrollTrigger: {
                        trigger: "#card-conteo",
                        start: "top 80%",
                        toggleActions: "play none none reset",
                    },
                },
            );
        });
    }
</script>

<!-- ═══════════════════════════════════════════════════════════ -->
<!-- 🎨 ESTILOS CSS                                              -->
<!-- ═══════════════════════════════════════════════════════════ -->
<style>
    /* Color de textos #122a4d */
    .texto-conteo {
        color: #122a4d;
    }

    /* Color del icono de regalo #122a4d */
    .icono-conteo-color {
        filter: brightness(0) saturate(100%) invert(13%) sepia(31%)
            saturate(1876%) hue-rotate(186deg) brightness(95%) contrast(96%);
    }

    /* Botón con color #122a4d */
    .btn-ubicacion-conteo {
        color: #122a4d;
        border-color: #122a4d;
    }

    .btn-ubicacion-conteo:hover {
        background-color: #122a4d;
        color: white;
        transform: scale(1.05);
        box-shadow: 0 5px 15px rgba(18, 42, 77, 0.3);
    }

    /* Efecto hover en la card */
    #card-conteo > div {
        transition: all 0.4s ease;
        transform-style: preserve-3d;
    }
</style>
