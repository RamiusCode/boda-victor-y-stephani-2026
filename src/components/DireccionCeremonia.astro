<!-- ═══════════════════════════════════════════════════════════ --><!-- ⛪ SECCIÓN: CEREMONIA                                       --><!-- ═══════════════════════════════════════════════════════════ -->
<section class="relative w-full pt-4 pb-12 sm:py-20 md:py-24 lg:py-32">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <!-- 📇 CARD DE CEREMONIA - HORIZONTAL -->
        <div class="flex justify-center">
            <div
                class="relative w-full max-w-md sm:max-w-lg md:max-w-xl"
                id="card-ceremonia"
            >
                <!-- BOTÓN DECORATIVO SUPERIOR -->
                <div
                    class="absolute z-20"
                    style="top: -30px; left: 50%; transform: translateX(-50%);"
                >
                    <div class="w-16 h-16" id="boton-decorativo-ceremonia">
                        <img
                            src="/imagenes/boton.png"
                            alt="Decoración"
                            class="w-full h-full object-contain"
                        />
                    </div>
                </div>

                <div
                    class="relative rounded-[32px] overflow-hidden card-roja"
                >
                    <!-- IMAGEN DE FONDO -->
                    <img
                        src="/imagenes/fondorojo.png"
                        alt="Fondo ceremonia"
                        class="absolute inset-0 w-full h-full object-cover"
                    />

                    <!-- Contenido de la card - LAYOUT CON TÍTULO EXTENDIDO -->
                    <div
                        class="relative px-4 pt-14 pb-8 sm:px-8 sm:pt-16 sm:pb-10 md:px-10 md:pt-16 md:pb-12 z-10"
                    >
                        <!-- TÍTULO: CEREMONIA RELIGIOSA - OCUPA TODO EL ANCHO -->
                        <div class="mb-2">
                            <h2
                                class="font-great-vibes text-4xl sm:text-4xl md:text-5xl texto-ceremonia text-center"
                                id="titulo-ceremonia"
                            >
                                Ceremonia Religiosa
                            </h2>
                        </div>

                        <!-- CONTENIDO INFERIOR CON ICONO Y TEXTOS -->
                        <div class="flex items-center justify-between gap-4">
                            <!-- LADO IZQUIERDO: ICONO DE IGLESIA -->
                            <div class="flex-shrink-0 ml-4 sm:ml-6 md:ml-8">
                                <div
                                    class="w-24 h-24 sm:w-20 sm:h-20 md:w-24 md:h-24 icono-iglesia-color"
                                >
                                    <img
                                        src="/imagenes/iglesia.png"
                                        alt="Iglesia"
                                        class="w-full h-full object-contain"
                                        id="icono-iglesia"
                                    />
                                </div>
                            </div>

                            <!-- LADO DERECHO: TEXTOS Y BOTÓN -->
                            <div class="flex-1 text-center">
                                <!-- NOMBRE DE LA PARROQUIA -->
                                <div class="mb-2">
                                    <p
                                        class="font-crimson text-xl sm:text-lg md:text-xl texto-ceremonia font-semibold"
                                        id="nombre-parroquia"
                                    >
                                        Capilla de <br />"El Montículo"
                                    </p>
                                </div>

                                <!-- DIRECCIÓN -->
                                <div class="mb-2 leading-tight">
                                    <p
                                        class="font-crimson text-xl sm:text-sm md:text-base texto-ceremonia"
                                        id="direccion-parroquia"
                                    >
                                        C. Méndez Arcos,<br />
                                        Parque Mirador, Sopocachi
                                    </p>
                                </div>

                                <!-- HORA -->
                                <div>
                                    <p
                                        class="font-crimson text-xl sm:text-base md:text-lg leading-tight texto-ceremonia font-semibold"
                                        id="hora-ceremonia"
                                    >
                                        Hora: 14:00 p.m.
                                    </p>
                                </div>
                            </div>
                        </div>

                        <!-- BOTÓN VER UBICACIÓN: fila propia a todo el ancho
                             para que el texto nunca se parta en dos líneas -->
                        <div class="mt-6 flex justify-center">
                            <a
                                href="https://maps.app.goo.gl/cqvnYtW5ZTYtjd3F7"
                                target="_blank"
                                rel="noopener noreferrer"
                                class="inline-block whitespace-nowrap px-6 py-2 sm:px-8 sm:py-2.5 border-2 rounded-full font-crimson text-base sm:text-sm uppercase tracking-wider transition-all duration-300 btn-ubicacion-ceremonia font-semibold"
                                id="btn-ubicacion"
                            >
                                VER UBICACIÓN
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- ═══════════════════════════════════════════════════════════ -->
<!-- 🎬 ANIMACIONES CON GSAP                                     -->
<!-- ═══════════════════════════════════════════════════════════ -->
<script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";

    gsap.registerPlugin(ScrollTrigger);

    if (typeof window !== "undefined") {
        document.addEventListener("DOMContentLoaded", () => {
            console.log("⛪ Iniciando animaciones de ceremonia...");

            gsap.fromTo(
                "#boton-decorativo-ceremonia",
                { opacity: 0, scale: 0, rotation: -180 },
                {
                    opacity: 1,
                    scale: 1,
                    rotation: 0,
                    duration: 1,
                    ease: "back.out(1.7)",
                    scrollTrigger: {
                        trigger: "#card-ceremonia",
                        start: "top 80%",
                        toggleActions: "play none none reset",
                    },
                },
            );

            // 🎯 ANIMACIÓN ELEGANTE DE ENTRADA - TODO AL MISMO TIEMPO
            gsap.fromTo(
                "#card-ceremonia",
                {
                    opacity: 0,
                    scale: 0.85,
                    y: 60,
                    rotationY: -15,
                },
                {
                    opacity: 1,
                    scale: 1,
                    y: 0,
                    rotationY: 0,
                    duration: 1.2,
                    ease: "power3.out",
                    scrollTrigger: {
                        trigger: "#card-ceremonia",
                        start: "top 80%",
                        toggleActions: "play none none reset",
                    },
                },
            );

            console.log("⛪✨ ¡Animaciones de ceremonia activadas!");
        });
    }
</script>

<!-- ═══════════════════════════════════════════════════════════ -->
<!-- 🎨 ESTILOS CSS                                              -->
<!-- ═══════════════════════════════════════════════════════════ -->
<style>
    /* Fondo rojo: cuerpo en blanco, titulo en dorado */
    .texto-ceremonia {
        color: #ffffff;
    }

    #titulo-ceremonia,
    #nombre-parroquia,
    #hora-ceremonia {
        color: var(--dorado);
    }

    /* Icono en dorado sobre el fondo rojo */
    .icono-iglesia-color {
        filter: brightness(0) saturate(100%) invert(67%) sepia(18%)
            saturate(650%) hue-rotate(357deg) brightness(114%) contrast(94%);
    }

    /* Botón en dorado sobre el fondo rojo */
    .btn-ubicacion-ceremonia {
        color: var(--dorado);
        border-color: var(--dorado);
    }

    .btn-ubicacion-ceremonia:hover {
        background-color: var(--dorado);
        color: #5f1b1c;
        transform: scale(1.05);
    }

    /* Efecto hover en la card */
    #card-ceremonia > div {
        transition: all 0.4s ease;
        transform-style: preserve-3d;
    }

    /* #card-ceremonia > div:hover {
        transform: translateY(-8px);
        box-shadow: 0 10px 10px rgba(95, 27, 28, 0.3);
    } */
</style>
